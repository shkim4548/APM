# DOCUMENTATION_PLAYBOOK.md — 포트폴리오 문서화 작업 재사용 가이드

> 2026-07-29 세션에서 `Docs/portfolio_apm_light.pptx`(18슬라이드 발표용 PPT)를 만들고
> 여러 차례 시각 개선 라운드를 반복하며 얻은 노하우를 정리한 문서.
> **다음에 이 저장소(또는 비슷한 성격의 포트폴리오 PPT/문서)를 다루는 세션은 작업 시작 전에 이 파일을 먼저 읽을 것.**
> `WORK_STATUS.md`의 "부가 산출물" 절에 있는 chronological 로그가 "무엇을 언제 했는지"라면,
> 이 문서는 "어떻게 하면 그 과정을 더 빠르고 정확하게 반복할 수 있는지"에 집중한다.

---

## 1. 가장 중요한 결정 — HTML→변환 대신 python-pptx 네이티브 생성

**겪었던 문제**: `Docs/portfolio_apm_light.html`(라이트 테마 웹 포트폴리오)의 팔레트를 그대로 재현한 Markdown 덱을 `npx @marp-team/marp-cli`로 만들고 `--pptx --pptx-editable`(LibreOffice 경유)로 변환하는 경로를 먼저 시도했었다. Chrome 렌더링(PNG 프리뷰)까지는 정확했지만, **LibreOffice의 HTML→PPTX 변환 단계에서 텍스트 런 경계의 숫자가 무작위로 소실되는 버그**가 실제로 재현됐다(`"AES-256-GCM"`→`"AES- -GCM"`, `"p95 34초"`→`"p 초"`, 슬라이드 제목의 숫자 전부 소실 등). marp-cli 공식 문서도 이 경로를 "EXPERIMENTAL, 재현성 보장 안 됨"이라 명시하고 있었다.

**결론/원칙**: 포트폴리오처럼 **숫자·사실관계가 틀리면 안 되는 문서**를 PPTX로 만들 때는, HTML/Markdown을 거쳐 무언가로 "변환"하는 파이프라인을 쓰지 말 것. `python-pptx`로 도형·텍스트박스를 직접 좌표 계산해서 그리는 게 처음엔 더 손이 가지만, 변환 단계 자체가 없어서 이런 클래스의 버그가 구조적으로 발생하지 않는다. `Docs/build_portfolio_apm_light_pptx.py`가 그 결과물이고, 이후 모든 개선은 이 스크립트를 직접 수정하는 방식으로 진행했다.

---

## 2. 표준 작업 루프 (매 변경마다 반복할 것)

1. 스크립트(`Docs/build_portfolio_apm_light_pptx.py`) 수정.
2. venv에서 재실행해 `.pptx` 재생성.
3. `soffice --headless --convert-to pdf`로 PDF 변환.
4. `pdftoppm -png -r 100`으로 **변경된 슬라이드만** PNG 렌더링(전체 18장을 매번 다 볼 필요 없음, 시간 낭비).
5. `Read` 도구로 PNG를 직접 열어 눈으로 확인 — **텍스트로 "잘 될 것 같다"고 판단하지 말고 반드시 렌더링 결과를 봐야 한다.** python-pptx는 좌표 기반이라 겹침/잘림이 코드만 봐서는 안 보인다.

```bash
# venv가 세션별 스크래치패드에 있어 매 세션 새로 설치해야 함(설치돼 있으면 생략)
python3 -m venv <scratchpad>/pptxvenv
<scratchpad>/pptxvenv/bin/pip install -q python-pptx Pillow

# 1) 재생성
cd Docs && <scratchpad>/pptxvenv/bin/python3 build_portfolio_apm_light_pptx.py

# 2) PDF 변환 + 3) 특정 슬라이드만 렌더링 (예: 4, 8, 9번만 확인)
cd <scratchpad>/pptx_check
soffice --headless --convert-to pdf Docs/portfolio_apm_light.pptx
pdftoppm -png -r 100 -f 4 -l 4 portfolio_apm_light.pdf s4
pdftoppm -png -r 100 -f 8 -l 9 portfolio_apm_light.pdf s89
```

**팁**: `-f`/`-l`로 페이지 범위를 좁혀서 렌더링하면 훨씬 빠르다. 레이아웃 헬퍼 함수(아래 §3)를 고치면 그 함수를 쓰는 슬라이드가 전부 영향받으므로, 변경 범위를 파악해서 관련 슬라이드를 전부(그리고 그것만) 재확인할 것.

---

## 3. 재사용 가능한 헬퍼 함수 카탈로그

스크립트 상단(`build_portfolio_apm_light_pptx.py` 100~535줄 부근)에 이미 만들어져 있는 헬퍼들. **새 슬라이드/새 시각 요소가 필요하면 먼저 이 목록에서 재사용 가능한 게 있는지 확인하고, 없을 때만 새로 만들 것** — 이번 세션에 만든 헬퍼 20여 개가 전부 "이 디자인 시스템 언어"(카드+왼쪽 액센트 바, 무채색 배경+포인트 컬러)를 공유하기 때문에, 새로 짜면 톤이 어긋나기 쉽다.

| 함수 | 용도 |
|---|---|
| `new_slide()` | 빈 슬라이드 + 배경색 세팅 |
| `rect(...)` | 둥근 사각형(카드/배지 배경) |
| `text(...)` / `multi_run(...)` | 단일/다중 서식 텍스트박스 |
| `lines_block(...)` | 여러 줄(각 줄이 독립 문단)을 한 번에 |
| `tag(...)` | 알약형 태그 배지(폭을 텍스트 길이로 자동 계산) |
| `page_num(...)` | 우하단 페이지 번호 |
| `title_block(slide, kicker, title, sub=None)` | 슬라이드 상단 제목 블록. **`kicker`는 "//" 없이 순수 텍스트로**(AI 티 난다는 피드백 이후 확정, §5 참고) |
| `decision_card(...)` | "문제/판단/해결"류 3단 미니카드 — 설계 결정, Round 요약 슬라이드에 재사용 |
| `bug_fix(...)` | 버그(빨강)/수정(초록) 카드, 왼쪽 컬러 바 + 미니카드 |
| `arch_box(...)` / `arch_arrow(...)` | 아키텍처 파이프라인 다이어그램(박스+화살표) |
| `screenshot_card(...)` | 실제 스크린샷을 letterbox로 삽입(크롭 대신 contain — 차트가 잘리면 안 되므로) |
| `tech_card(...)` | 기술 스택 카드 — 하위 항목을 flow-wrap 미니카드로 |
| `code_block(...)` | 문법 하이라이트된 코드 스니펫 |
| `bar_chart_two_series(...)` | 2계열 그룹 막대 차트 |
| `stat_block(...)` | 큰 숫자 + 라벨(hero stat, contact stat 등에서 재사용) |
| `callout(...)` | 초록 하이라이트 박스 |
| `table(...)` | 표(헤더/셀 폰트 크기 파라미터화됨) |

**폰트 관련 특수 처리** — `_script_segments()`/`_add_text_runs()`: `Noto Sans Mono CJK KR`은 라틴 문자·숫자도 한글 전각 폭으로 그려서 영문 코드/제목이 헐렁해 보이는 문제가 있었다. 문자열을 한글/비한글 구간으로 쪼개 비한글 구간엔 `Noto Sans Mono`(비CJK 전용, 좁은 라틴 폭)를 쓰도록 만든 헬퍼 — `FONT_MONO`를 쓰는 모든 텍스트 함수(`text`/`multi_run`/`lines_block`)에 이미 내장돼 있으므로 직접 호출할 일은 없지만, "왜 이렇게 짜여 있는지" 알아둘 것.

**줄바꿈/높이 추정** — `_text_width_in()`/`_est_lines()`: 카드 높이를 미리 계산하려면 텍스트가 몇 줄이 될지 추정해야 하는데, 단순 `len(text)/chars_per_line` 방식은 **한글 비중이 높은 문장에서 실제 렌더 폭을 과소평가해 텍스트가 카드 밖으로 잘리는 버그**를 냈다(9~12번 슬라이드에서 실제 발생). CJK는 전각(~2배 폭), 라틴은 반각으로 계산하는 `_text_width_in()` 기반 `_est_lines()`로 교체해서 해결 — **새로 높이-가변 카드를 만들 때는 반드시 이 함수를 재사용할 것**, 직접 문자 수 세는 방식으로 새로 짜지 말 것.

---

## 4. 반복하며 실제로 겪은 문제 → 해법 패턴

이 프로젝트에서 "한 번에 완벽하게" 나온 슬라이드는 거의 없었다. 아래는 실제로 반복하며 확인된 패턴 — 새 문서 작업에서도 같은 종류의 문제가 먼저 나올 가능성이 높다.

1. **여백 불균형(상단은 좁고 하단이 텅 빔)**: 콘텐츠를 위에서부터 순서대로 쌓기만 하고 슬라이드 전체 높이(7.5in)를 채우도록 설계하지 않으면 생김. → 해법: 각 요소 배치 후 남는 공간에 실제 콘텐츠(스탯 스트립, 구분선+콜아웃, 차트)를 추가로 채우거나, 카드 자체를 확대. 빈 여백을 여백인 채로 두지 말 것.
2. **콜아웃/하이라이트 박스가 슬라이드마다 다른 위치**: `cy`(현재 y좌표) 뒤에 바로 이어 붙이면 콘텐츠 길이에 따라 위치가 들쭉날쭉해짐 → 해법: 페이지 번호 근처에 **고정 앵커**(`Inches(6.95) - h`)로 통일해서 "푸터"라는 일관된 위치 감각을 줌.
3. **카드 안 긴 문단이 안 읽힘**: "문제/판단/해결" 같은 라벨+긴 문장이 한 덩어리로 있으면 스캔하기 어려움 → 해법: 문장을 의미 단위로 쪼개 각각 라벨 달린 미니카드로 분리(`decision_card`/`bug_fix`가 이 패턴).
4. **번호 킥커("// 01")가 AI가 만든 티가 남**: 사용자 피드백으로 전부 제거하고 "01. 제목" 형태로 번호를 제목에 병합. **앞으로 새 슬라이드를 만들 때도 이 스타일(따로 떨어진 "//" 주석형 라벨)은 쓰지 말 것.**
5. **모노스페이스 폰트에서 라틴 자간이 벌어짐**: §3의 `_script_segments` 참고.
6. **화살표 라벨이 박스 제목과 겹침**: 화살표 캡션을 박스와 같은 y좌표에 겹쳐 그리면 생김 → 해법: 라벨 전용 상단 밴드(`label_y`/`label_h`)를 따로 배정해서 절대 겹치지 않게 함(`arch_arrow` 참고).
7. **레이아웃 함수를 고치면 여러 슬라이드가 동시에 영향받음**: 공용 헬퍼(`decision_card`, `bug_fix` 등)를 수정하면 그 함수를 쓰는 모든 슬라이드를 다시 렌더링해서 확인해야 함 — 한 슬라이드만 보고 "됐다"고 넘기면 다른 슬라이드에서 회귀가 생길 수 있음.

---

## 5. 사용자 피드백 기반 확정 스타일 규칙 (다음에도 그대로 적용할 것)

- **킥커/라벨에 `//` 주석 스타일 금지** — "너무 AI가 만든 티가 난다"는 명시적 피드백. 순수 텍스트나 "01. 제목" 병합 형태만 쓸 것.
- **카드 내부 번호는 라벨 형태로 따로 떼지 말고 제목에 병합**(`f"{num}. {title}"`).
- **스크린샷은 crop이 아니라 letterbox(contain)로 삽입** — 대시보드 차트처럼 잘리면 안 되는 콘텐츠가 있을 수 있어서, 원본 비율을 유지하며 남는 공간은 여백으로 둔다.
- **버그 카드는 배경 전체를 색칠하지 않는다** — 경고창처럼 보여서 절제되지 않은 인상을 줌. 배경은 중립색 고정 + 왼쪽 얇은 컬러 바만.
- **차트/팔레트 색상은 감으로 고르지 않는다** — `dataviz` 스킬 로드 후 `scripts/validate_palette.js`로 실제 CVD/normal-vision ΔE를 검증한 값만 채택.

---

## 6. 콘텐츠 정확성 검증 — 시각 작업이 끝났다고 끝난 게 아니다

시각 개선을 여러 라운드 반복한 뒤, 별도로 "이 문서에 있는 **사실**이 실제로 맞는가"를 검증하는 라운드가 반드시 필요했다. 실제로 이 마지막 검증에서만 발견된 문제:

- 슬라이드 순서가 자체 주장("순서대로 추가")과 실제 이력이 어긋남 (WORK_STATUS.md 로드맵 순서와 대조해서 발견).
- "5라운드"라는 숫자가 라벨링된 카드 수(4개)와 안 맞아 보였는데, 원본 HTML을 대조해보니 5번째 라운드가 라벨만 누락된 것이었음(HTML도 같이 봐야 확정할 수 있었던 문제).
- 페이지 상호참조("8~12p 참고")가 실제로 그 내용을 담고 있는 페이지 범위와 어긋남.
- 이미 지웠다고 생각한 "//" 스타일이 차트 캡션·다른 카드 그룹 제목 등 놓친 곳에 남아있었음.

**검증 절차(다음에도 그대로 따를 것)**:
1. 스크립트에 있는 모든 숫자·기술적 주장을 실제 소스 코드(`grep`/`Read`로 직접 열어서)와 1차 문서(`PROJECT_TECHNICAL_REVIEW.md`, `WORK_STATUS.md`)에 대조.
2. 코드 스니펫을 인용했다면 **원본 파일과 바이트 단위로 비교**(요약/각색이 아니라 정말 그 파일에 그 코드가 있는지).
3. "A와 B의 순서/개수/범위가 서로 맞는가" 같은 **내부 정합성**도 따로 체크 — 개별 사실은 맞아도 슬라이드 간 상호참조가 어긋날 수 있음.
4. 이 저장소는 `PROJECT_TECHNICAL_REVIEW.md` §0에 "대외적으로 '게임'이라는 단어 금지" 같은 포지셔닝 규칙이 있음 — 새 문서를 만들 때마다 이런 프로젝트 고유의 금기어/포지셔닝 규칙이 있는지 먼저 확인할 것.
5. 링크(상대경로 파일 참조)와 섹션 앵커(`§7-4` 같은 참조)도 실제로 존재하는지 파일시스템에서 직접 확인 — 문서가 여러 라운드에 걸쳐 수정되면서 섹션 번호가 밀렸을 수 있음.

---

## 7. 다음에 문서화 작업을 시작할 때 체크리스트

1. 이 파일과 `WORK_STATUS.md`의 "부가 산출물" 절을 먼저 읽는다.
2. 기존 스크립트(`Docs/build_portfolio_apm_light_pptx.py`)를 재사용/확장할지, 새로 만들지 판단 — **재사용 가능하면 반드시 재사용**(§3의 헬퍼 카탈로그가 이미 검증된 디자인 언어).
3. venv 재설치(§2) → 스크립트 수정 → 재생성 → 렌더링 → 눈으로 확인, 이 루프를 작은 단위로 반복.
4. 시각 개선이 끝났다고 판단되면, 반드시 별도로 §6의 콘텐츠 정확성 검증 라운드를 거친다 — 시각 검증과 사실 검증은 별개의 패스로 취급할 것.
5. 마지막에 이 문서 자체도 필요하면 갱신 — 새로운 패턴/함정을 발견했다면 §4·§5에 추가해서 다음 세션이 같은 시행착오를 반복하지 않게 한다.
