# CLAUDE.md — 작업 규칙

> 원 모노레포 `gw2-cross/CLAUDE.md`의 규칙을 이 저장소(`APM`) 구조에 맞게 이어받음(2026-07-26).
> `GW2_ServerCore`(MSVC++ 원본) 비교 규칙, `ANALYSIS_STATUS.md` 분석 트랙 규칙은 이 저장소에 해당 대상이 없어 제외.

## 필수 규칙

### 1. 작업 시작 전 반드시 WORK_STATUS.md를 읽어라
- 모든 작업 세션 시작 시 `WORK_STATUS.md`를 먼저 읽는다.
- 현재 단계, 다음 할 일, 중단 이유를 확인한 뒤 작업을 시작한다.
- 작업 완료 또는 중단 시 `WORK_STATUS.md`를 업데이트한다.

### 2. 코드를 직접 수정하지 마라
- 코드 파일(.h, .cpp, .cs 등)을 Claude가 직접 편집하지 않는다.
- 분석, 설계, 방향 제시만 수행하고 실제 코드 수정은 사용자가 직접 한다.
- 단, 사용자가 명시적으로 "수정해라" / "작성해라" 라고 요청한 경우에만 예외로 한다.

### 3. 제안 시 코드 조각과 변경 사유를 함께 제시하라
- 수정 제안 시 반드시 **수정 전 / 수정 후** 코드 조각을 나란히 보여준다.
- 각 제안에 **변경 사유**를 명시한다(어떤 문제인지, 왜 이 방식으로 대체하는지).

### 4. 코드 조각의 범위를 지켜라
- 수정 대상이 **멤버 변수**인 경우 → 해당 클래스의 **멤버 변수 선언 전체**를 함께 보여준다.
- 수정 대상이 **함수**인 경우 → 해당 **함수 전문(全文)**을 보여준다.
- 일부만 발췌하여 `...` 로 생략하지 않는다.

### 5. 모든 답변 마지막에 정리 섹션을 넣어라
- 답변 말미에 반드시 **정리** 섹션을 작성한다.
- 내용: 이번 답변에서 다룬 내용 요약, 완료/미완료 항목, 다음으로 해야 할 일.
- 형식 예시:
  ```
  ## 정리
  - 다룬 내용 : ...
  - 완료 항목 : ...
  - 다음 할 일 : ...
  ```

### 6. 코드가 포함된 답변은 Docs/SESSION_LOG.md에 기록하라
- VSCode 확장 채팅 패널/CLI 터미널의 여러 줄 코드 블록 렌더링 품질 문제(한 줄씩 쪼개져 보임)를 우회하기 위함.
- 코드 조각이 포함되는 답변은 `Docs/SESSION_LOG.md`에 항목을 추가(append)하는 방식으로 기록한다. 최신 항목은 파일 아래쪽에 시간순으로 쌓는다.
- 코드는 예외 없이 \`\`\` 코드 블록으로 감싸고, 언어 태그(예: \`\`\`cpp, \`\`\`cmake)를 명시한다. 본문 설명과 코드는 명확히 분리한다.
- 채팅 답변 본문에는 무슨 작업을 했는지 요약만 남기고, "자세한 코드는 Docs/SESSION_LOG.md 참고"로 안내한다 — 채팅창에 코드 전문을 다시 중복 출력하지 않는다.

### 7. "현황 최신화" 요청 시 대화 기록이 아니라 실제 파일을 직접 읽어라
- 사용자가 "현황 최신화해줘" / "현황 파악해줘" 등을 요청하면, 이전 대화에서 제안했던 코드 내용이나 기억에 의존하지 말고 **그 시점에 실제 디스크에 있는 파일**(코드, CMake 설정, WORK_STATUS.md, 관련 스키마/설정 파일 등)을 다시 읽어서 확인한 뒤 답변한다.
- 제안한 코드와 사용자가 실제로 적용한 코드는 다를 수 있고, 사용자가 직접 만든/수정한 파일이 이미 존재할 수도 있으므로, 항상 디스크의 현재 상태를 1차 근거로 삼는다.
- `WORK_STATUS.md`의 로드맵/체크리스트 표도 이 시점에 함께 대조해서 상태 표시(대기/진행/완료)를 실제와 맞게 갱신한다 — "현재 작업 현황" 서술 섹션만 갱신하고 상단 체크리스트 표를 방치하지 않는다.
